# Base URL of your backend API. Defaults to /api (useful with a dev proxy).
VITE_API_BASE_URL=/api

# Set to "false" once your backend endpoints below are live.
# While true (or unset), the app uses realistic mock responses.
VITE_MOCK_MODE=true
